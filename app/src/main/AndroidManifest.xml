<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET"/>
<!--    <uses-permission android:name="luci.sixsixsix.powerampache2.permission.BIND_INFO_PLUGIN" />-->


    <application
        android:name=".PluginApplication"
        android:networkSecurityConfig="@xml/network_security_config"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/ic_info"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_info"
        android:supportsRtl="true"
        android:theme="@style/Theme.PowerAmpache2"
        tools:targetApi="35" >

        <activity android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
            </intent-filter>
        </activity>

        <service
            android:name="luci.sixsixsix.powerampache2.infoplugin.InfoFetcherService"
            android:exported="true"/>
    </application>

</manifest>
